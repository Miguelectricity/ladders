 - range not supported
 - location must be parsed
 - approval rules should be more dynamic policies
 - usd criteria provided in requirements are only for usd checks or only for hourly checks if provided. If not in USD then we won't reject.
 - sorting only by a single currency type

to-do:
- location parsing manual
- ingestion failure
- readme with setup
- id hash
- long lists (pagination, virtualization)
- imports and package resolve